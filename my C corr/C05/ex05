#include <stdio.h>
int ft_sqrt(int nb)
{
	int to_find;

	to_find = 1;

	while (to_find <= nb)
	{
	if (to_find * to_find == nb)
	{
		return (to_find);
	}
	to_find++;
	}
	return (0);
}
int main()
{
	int n;
	printf("saisir number   :  ");
	scanf("%d",&n);
	printf("\nsquar is : %d",ft_sqrt(n));
}
